# Project Summary

This work covered sequence modeling for machine translation:

- Naive RNN and LSTM implementations.
- Encoder-decoder Seq2Seq model.
- Transformer architecture components.
- Training utilities and evaluation workflow.

The original course files contained explicit warnings against public redistribution, so the public portfolio keeps this summary, environment metadata, and non-restricted final-project experiment scripts rather than publishing the restricted assignment implementation files.
